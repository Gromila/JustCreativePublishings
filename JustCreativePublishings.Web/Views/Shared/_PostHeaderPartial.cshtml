@model JustCreativePublishings.Web.Models.PostViewModel
           
<a href="@Url.Action("Show", "Post", new { id = Model.Id })" class="list-group-item">
    <h4 class="list-group-item-heading">@Model.Title</h4>
    <p class="list-group-item-text">@Model.ShortDescription</p>
</a>
